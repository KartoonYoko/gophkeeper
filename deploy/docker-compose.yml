services:
  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=gophkeeper
      - MINIO_ROOT_PASSWORD=supersecret
    ports:
      - 9000:9000
      - 9001:9001
    volumes:
      - minio-vm:/data

volumes:
  minio-vm: